[build-system]
requires = [
    "setuptools>=61.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "clam"
version = "0.0.1"
description = "CLAM"
readme = "README.md"
authors = [
    {name = "Mahmoodlab"},
]